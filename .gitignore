# 대용량 데이터셋 / 산출물 (git에 올리지 않음)
pav_sample/
visionary_anns/
results/

# 모델/허깅페이스 캐시
.cache/

# python
__pycache__/
*.pyc

# 문서 (필요시 별도 공유)
VLM 기반 반자동 edge-case mining을 위한 시나리오 분류.docx

# 로그
run_all.log
*.log

# 다운로드 임시 캐시/상태
.hf_download_cache/
.download_state.json

# GUI 서버 로그 (client/ 실행파일이 nohup 으로 남긴다)
gui.log

# NAS 전송 대기용 청크 스테이징 (개당 ~1.2GB, GitHub 100MB 한도 초과)
# 다운로드 중간 산출물이라 git 에 올릴 이유가 없다.
.stage/

# 데이터 청크 zip - 어디에 있든 추적하지 않는다
*.chunk_*.zip

# 인덱스 캐시 (clip_source.py 가 로컬에서 다시 만든다)
.clip_index.json
*.clip_index.json.lock

# 단일 클립 조회가 만드는 uuid 파일 (job id 로 쌓인다)
gui/single/
